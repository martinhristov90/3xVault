#!/usr/bin/env bash

# This does nothing for now

# No need to write the license to each node, it is stored in the backend storage

# Debugging purposes
echo "$(date +"%A, %b %d, %Y %H:%M:%S") - This is a follower node, it joins to active node" > /tmp/vault_mode